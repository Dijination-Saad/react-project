import React from "react";
import styles from "../styles/style";

const Footer = () => {
  return (
    <div>
      <h1 className={`${styles.heading4} text-curious-blue`}>Footer</h1>
    </div>
  );
};

export default Footer;
